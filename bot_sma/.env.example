# ============================================================
# Бот: SMA crossover (пересечение скользящих средних).
# Это базовый бот - используйте как шаблон.
# ============================================================

# ============ Bybit API ключи ============
BYBIT_API_KEY=your_api_key_here
BYBIT_API_SECRET=your_api_secret_here
# Тестовая сеть (true = testnet, false = mainnet)
TESTNET=true

# Режим симуляции: true - бот НЕ отправляет реальные ордера,
# только логирует, что бы сделал. Безопасно для отладки.
SIMULATION_MODE=true

# Рыночная категория: linear (USDT-фьючерсы) | spot
CATEGORY=linear

# Торговая пара
SYMBOL=XRPUSDT

# Таймфрейм свечей: 1 | 5 | 15 | 60 | 240 | D
TIMEFRAME=5

# Интервал проверки сигналов (секунды)
POLL_INTERVAL=60

# Сколько свечей запрашивать для расчёта индикаторов
KLINE_LIMIT=200

# Максимум REST-запросов в секунду (защита от лимитов Bybit)
REQUESTS_PER_SECOND=10

# ============ Риск-менеджмент ============
# Размер позиции в % от доступного баланса
POSITION_PCT=100.0

# Фиксированный размер лота в базовом активе.
# 0 = выключено (размер считается от POSITION_PCT).
FIXED_QTY=0

# Стоп-лосс и тейк-профит в % от цены входа.
# ВНИМАНИЕ: для этого бота обычные процентные стопы.
STOP_LOSS_PCT=2.0
TAKE_PROFIT_PCT=4.0

# ============ WebSocket ============
WS_ENABLED=true

# ============ Telegram уведомления ============
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ============ Логирование ============
LOG_LEVEL=INFO
LOG_FILE=logs/sma.log
STATE_FILE=data/state_sma.json

# ============ Параметры стратегии SMA ============
# Периоды быстрой и медленной скользящих средних
FAST_MA_PERIOD=3
SLOW_MA_PERIOD=10
