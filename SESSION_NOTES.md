# SESSION_NOTES.md

### ВАЖНО — договорённость с пользователем
- Скринеры переведены на мейннет (TESTNET=false).
- Мейннет ключи: kOFDh0YrjjbkiC8VSp (пользователь вписал).
- Пороги скринеров адаптированы под мейннет.
- Ботов НЕ запускать — пользователь скажет когда.
- Стратегии переписываем с нуля — пользователь сам решает что в каждом боте.

### Что сделано (сессия 2026-09-06)
- **Исправлен PUMP скринер** (был 0 сигналов):
  - Веса факторов: все 0.0 → 15/20/15/10/20/0/5/10 (outflow=0, нет on-chain)
  - `MAX_PRICE`: $0.03 → $0.50
  - `MIN_TOTAL_VOLUME_USD`: $50M → $10M
  - Результат: 37 сигналов (WLD 64.8%, ZAMA 57.8%, SUSHI 56.4%)
- **Исправлен UZKIY скринер** (был 0 сигналов):
  - `MIN_TURNOVER_24H`: $30M → $5M
  - `SCAN_INTERVAL`: 60с → 300с
  - Результат: 1135 сигналов (ZEC, ARB, HYPE — ОЧЕНЬ УЗКИЙ)
- **Исправлен KRUGLOE скринер** (был 0 сигналов):
  - `MIN_TURNOVER_24H`: $10M → $3M
  - `PROXIMITY_PCT`: 5% → 8%
  - `MAX_PRICE`: $30 → $50
  - Результат: 680 сигналов (VVV 0.04%, VIRTUAL 0.11%, EGLD 0.16%)
- **Исправлен capture-баг в `run_all_screeners.py`**:
  - Все `main.py` делают `from printer import print_results` — прямая ссылка
  - Замена `printer_mod.print_results` не влияла на уже импортированную ссылку
  - Добавлен патч `main_mod.print_results = _capture_print` для всех 6 скринеров
- Исправлен баг в шаблоне отчёта krugloe: `s.round_level` → `s.level`, удалён `s.level_type`

### Результат запуска всех скринеров (2026-09-06, 1 час, мейннет)
**Отчёты:** `C:\Users\79095\Desktop\trd\Отчеты скринера\`

| Скринер | Было (05.09) | Стало (06.09) | Интервал |
|---|---|---|---|
| PUMP | **0** | **37** | 3600с |
| UZKIY | **0** | **1135** | 300с |
| KRUGLOE | **0** | **680** | 300с |
| KLIN | 133 | 188 | 10с |
| YROVNI | 120 | 163 | 300с |
| IMPULSE | 155 | 141 | 15с |

### Текущая структура проекта
```
TRD bot/
├── core/                  # Общий движок (11 модулей, не трогаем)
├── bot_flat/              # Боковик
├── bot_yrovni/            # Уровни
├── bot_trend/             # Тренд
├── bot_impulse/           # Импульс
├── bot_0/                 # Нулевой
├── bot_screener_pump/     # Скринер накопления (мейннет, 37 сигналов)
├── bot_screener_klin/    # Скринер клинов (мейннет, 188 сигналов)
├── bot_screener_uzkiy/    # Скринер узкого диапазона (мейннет, 1135 сигналов)
├── bot_screener_yrovni/   # Скринер Volume Profile (мейннет, 163 сигнала)
├── bot_screener_krugloe/  # Скринер круглых чисел (мейннет, 680 сигналов)
├── bot_screener_impulse/  # Скринер импульсов (мейннет, 141 сигнал)
├── run_all_screeners.py   # Запуск всех скринеров на 1 час + отчёты
├── backtest.py            # Бэктест (ждёт стратегий)
├── launcher.py            # Фоновый запуск
├── tests/                 # 26 тестов
└── docker-compose.yml     # 5 сервисов
```

### Текущие настройки скринеров (мейннет)
| Параметр | pump | uzkiy | krugloe | klin | yrovni | impulse |
|---|---|---|---|---|---|---|
| TESTNET | false | false | false | false | false | false |
| TIMEFRAME | D | 1 | 5 | 1,3,5 | — | 1 |
| MIN_TURNOVER | $5M | $5M | $3M | $10M | $50M | $50M |
| SCAN_INTERVAL | 3600с | 300с | 300с | 10с | 300с | 15с |
| LOOKBACK_BARS | 100 | 60 | — | 80 | 500 | 100 |
| MAX_PRICE | $0.50 | — | $50 | — | — | — |
| EXCLUDE | 10 топ | 15 топ | 15 топ | 15 топ | 10 топ | 15 топ |

### Расшифровка отчётов скринеров

**ZAKONOMER** — поиск закономерностей во времени спайков объёма:
- `Spikes: X/Y` — X спайков из Y свечей
- `Threshold: $N` — динамический порог (95-й перцентиль)
- `Hours: HH:00(Nx)` — часы, когда чаще всего происходят спайки (паттерн)
- Топ-3 спайка — самые крупные выбросы с датой/временем/суммой/ценой
- Суть: ищет пары, где спайки повторяются в одни и те же часы

### Известные проблемы
- IMPULSE/YROVNI: дублируют одинаковые сигналы каждые 15-300с → нужна дедупликация

### План: скрипт-анализатор импульсов (2026-09-08)
- **Цель:** на основе реальных графиков с TradingView определить оптимальные параметры `ImpulseConfig`
- **Идея:** скрипт подключается к Bybit API, тянет свечи по заданному символу/timeframe, считает реальные `volume_ratio`, `candle_width`, `delta_ratio` для импульсных свечей
- **Результат:**提议ит оптимальные пороги для `volume_spike_multiplier`, `candle_width_min/max`, `delta_spike_multiplier`
- **Статус:** ожидает — пользователь должен скинуть пример графика или символ/timeframe для анализа

### Переписан UZKIY скринер (2026-09-08)
- **Было:** поиск узких свечей по min/max candle_pct + outliers
- **Стало:** поиск quiet zones через SMA-based фильтры
- **Параметры (из анализа AKEUSDT 08.09.26 + 07.09.26):**
  - `volume_max_ratio=2.2` — нет всплесков объёма
  - `candle_width_max=0.59` — все свечи узкие
  - `delta_max_ratio=3.4` — нет дельтовых шоков
- **Логика:** если ВСЕ свечи в окне узкие + нет шоков → сигнал quiet zone → ждём взрыв
- **Файлы:** `scanner.py`, `printer.py` обновлены
