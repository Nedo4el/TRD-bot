# SESSION_NOTES.md — контекст работы (продолжить с этого)

## Проект
TRD bot — торговый бот для Bybit (Python, pybit API v5). Стратегия SMA-пересечение — ВРЕМЕННАЯ, пользователь её использовать не будет, важна только механика работы (ордера, SL/TP, состояние). Позже будет другая стратегия с тонкой настройкой.

## Текущий статус (18.08.2026)
- Бот ЗАПУЩЕН в фоне (Start-Process, не привязан к терминалу, Ctrl+C не работает — останавливать через kill процесса main.py).
- Пара: **XRPUSDT**, таймфрейм **5m**, SMA **3/10** (специально сделано чувствительнее, чтобы быстрее увидеть сделку), категория **linear**.
- **TESTNET=false, SIMULATION_MODE=false** — реальные деньги! Ключи в .env — **mainnet** (те же, что пользователь использует в терминале Tiger Trade).
- Баланс: ~8.84 USDT. POSITION_PCT=100 (только так ордер проходит мин. notional ~5 USDT). SL 2%, TP 4% (макс. убыток ~$0.18).
- Сделок пока не было — сигнал всё время hold.

## Цель пользователя
1. Убедиться, что бот открывает ордера и ставит SL/TP.
2. **Увидеть сделки наглядно в Tiger Trade** (терминал подключается только к mainnet — поэтому работаем на mainnet).

## Технические решения в этой сессии
- CATEGORY=linear, SYMBOL в .env (пустой запрещён валидацией config.py).
- В bybit_client.py добавлено: округление qty ордера вниз до qtyStep биржи (get_instruments_info + кэш `_qty_steps`, `_round_qty`) — без этого Bybit отклоняет некратные объёмы.
- Проверено: ключи mainnet (на testnet 401).
- Запуск/перезапуск бота в фоне: убить процессы main.py → `$env:LOG_LEVEL="DEBUG"; Start-Process uv run python main.py ...` (kill и start в ОДНОЙ команде не работают — разделять!).
- Логи: logs/bot.log (DEBUG активен через env-переменную процесса), stdout/stderr в logs/.

## Как прогнать бэктест (пользователь спрашивал)
```
uv run python backtest.py --limit 1000
```
Или с переопределением параметров: `$env:SYMBOL="..."; $env:TIMEFRAME="5"; ...; uv run python backtest.py --limit 1000`

## Дальше (когда продолжим)
1. Дождаться первого сигнала на XRPUSDT 5m — проверить: ордер открылся, SL/TP выставлены на бирже, позиция видна в Tiger Trade.
2. Если ордер не откроется — смотреть ошибку (возможные причины: мин. notional, леверидж, режим позиций hedge/one-way).
3. Потом — замена стратегии на нужную пользователю (менять strategy.py:generate_signal, вызов в main.py:245).